name: Greeting
on: push
# on:
#     push:
#         branches:
#             - main
#             - 'feature/**'
jobs:
    test:
        runs-on: ubuntu-latest
        steps:
            - name: "Get code"
              uses: actions/checkout@v4
              with:
                  node-version: 20